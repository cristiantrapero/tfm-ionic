import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.cristiantrapero.molora',
  appName: 'molora',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
